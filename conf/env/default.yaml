# Deck CSVs are defined in the scheduler-independent paths config so storage
# can be redirected without changing env settings.
deck0: ${paths.deck0}
deck1: ${paths.deck1}
# Multi-deck training curriculum. When deck_pool is null the env uses the fixed deck0/deck1 pair
# above. When set to a directory, glob, or list of deck CSVs, each episode instead samples a matchup
# from the pool, so the policy trains across archetypes instead of overfitting one card pool.
# NOTE: The multi-deck training curriculum weighting and probabilities may need tweaks over time.
deck_pool: null
# How the two seats relate when sampling from a pool:
#   mirror      -> both seats get the same deck (isolates skill from matchup).
#   independent -> each seat drawn separately (cross-archetype matchups).
deck_matchup: mirror
# Matchup used for the periodic evaluator only (deck_split="eval"); null falls
# back to deck_matchup. Set this to "mirror" while training on "independent" so
# the eval win-rate reflects piloting skill rather than deck-vs-deck luck.
eval_deck_matchup: null
# Timesteps between sampling new decks from the pool. 0 samples on every reset.
deck_switch_steps: 0
# Draw strategy over the pool:
#   uniform     -> each deck equally likely, i.i.d. per episode.
#   round_robin -> deterministic rotation covering every deck evenly.
deck_sampling: uniform
# Draw strategy for the periodic evaluator only. "null" falls back to deck_sampling.
# Set this to "round_robin" so eval deterministically and evenly covers the held-out pool:
# the per-archetype breakdown then reflects the policy, not the luck of which held-out decks
# a uniform draw sampled that round.
eval_deck_sampling: null
# Probability that a training episode is a mirror match (both seats the same
# deck); the rest are independent draws. null derives it from deck_matchup
# (mirror -> 1.0, independent -> 0.0). Set e.g. 0.5 to mix the two, or sweep it
# across runs to interpolate between them. Training only; eval uses its matchup.
deck_mirror_prob: null
# Bias the training deck draw via manifest.json, so the policy faces a
# meta-realistic field instead of every list equally often:
#   null        -> uniform (every deck equally likely).
#   observation -> weight by the list's observation_count, so a list is dealt
#                  in proportion to how often it was actually seen in play.
#   winrate     -> weight by tournament record W/(W+L).
#   placing     -> weight by 1/placing (better finishes sampled more).
# Weighting is per *list*, not per archetype: counts are never summed across
# lists, each manifest entry standing for the one list it describes. In
# heuristic-resolved that spreads 50,485 observations over 28,670 lists, of
# which 76.7% were seen exactly once and hold 43.6% of the mass -- so this
# sharpens the draw toward the few heavily-played lists (the top 100 hold
# 11.8%) without collapsing onto them.
# Applies to the plain pool draw and to the curriculum's within-archetype deal
# alike. Training only; evaluation always samples the held-out pool uniformly.
deck_weighting: null
# Fraction of the pool held out from training and used only for evaluation, so
# eval measures generalization to unseen decks. 0.0 trains on the whole pool.
deck_holdout_frac: 0.0
# Cap the training pool to this many archetypes -- i.e. how many of the corpus's
# per-archetype folders training may draw from at all, the rest being dropped
# entirely. The held-out eval set is left untouched, so narrowing training is
# still measured against the full field. null uses every training archetype.
#
# This is the knob that decides how much training each matchup gets. A
# curriculum level is an ordered (agent, opponent) archetype pair, so the level
# space is the square of this: 134 archetypes is 17,956 matchups and ~11
# episodes each over a 15M-frame run, while 12 is 144 matchups and ~1,425 each.
#
# Sweep it across runs (--multirun env.deck_pool_width=4,8,16,32) to measure how
# held-out generalization scales with training deck diversity; the pools are
# nested as it grows, so a sweep adds archetypes rather than resampling them.
deck_pool_width: null
# Which archetypes deck_pool_width keeps:
#   observation -> the most-observed ones, by summed manifest observation_count.
#                  This is the only place counts are summed per archetype; the
#                  sampling weights in deck_weighting stay strictly per-list.
#   random      -> a seeded random subset, deterministic given deck_split_seed.
# Over the scraped corpus the top 12 archetypes hold 55.2% of all observed play,
# where a random 12 hold a median of 8.6% -- so 'random' answers "how does
# diversity scale" and 'observation' answers "train on what the ladder deals".
deck_pool_selection: observation
# Pin the agent to one deck and draw only its opponent from the pool. null
# trains both seats from the corpus.
#
# The submitted agent plays exactly one list, so a symmetric pool spends all
# but 1/archetypes of its episodes on decks the agent will never pilot, and
# spreads the rest over archetypes^2 matchups -- measured on run 049zz7kg,
# 134 archetypes gave 17,956 matchups 2.8 episodes each over 50,317 episodes,
# which is why train/curriculum/size reached only 97 with 11,692 still in
# probation. Pinning one seat leaves the opposing field at full width (that is
# the ladder the agent is scored against) and collapses the space to one
# matchup per opponent archetype, a few hundred episodes each.
#
# Incompatible with env.curriculum.enabled; see make_env_factories.
agent_deck: null
# Share of training episodes that ignore the pin and take both seats from the
# pool. Without some the card embeddings and per-card statics outside the
# pinned list stop receiving gradient, degrading the encoder on exactly the
# cards the opponent plays. Evaluation always uses 0.0 so the eval curve
# measures the deck that will be submitted.
agent_deck_field_prob: 0.2
# Deck the agent pilots during EVALUATION only, independent of agent_deck.
#
# Evaluation should hold the agent's deck fixed even when training does not: the
# curve is meant to read like a submission, which is one deck against a field.
# It is also what makes the per-opponent breakdown meaningful, since the agent's
# own archetype is then constant and the opponent's is the only thing varying.
#
# Unlike agent_deck this costs nothing: evaluation does not train, so a fixed
# eval deck cannot degrade the self-play opponent the way a training pin does.
eval_agent_deck: null
# Restrict evaluation opponents to the N most-observed held-out lists, drawn
# round-robin, instead of dealing a fresh list every episode.
#
# Without it a 50-episode round is 50 different matchups played once each, and
# which decks the draw happened to hit swamps the policy signal. A fixed panel
# plays the same matchups every round: the curve is comparable across rounds and
# each opponent accumulates enough episodes to be read on its own. Set
# eval_deck_sampling: round_robin alongside it for even coverage.
#
# null evaluates against the whole held-out pool (the old behaviour), which is
# the less noisy choice only when eval_episodes is large.
eval_panel_size: null
# Seed for the deterministic train/holdout split.
deck_split_seed: 0
# Prioritized Level Replay over deck-archetype matchups. When enabled, each
# episode's matchup is drawn from a distribution the learner maintains, rather
# than uniformly from the pool, so training concentrates on the matchups the
# critic still misjudges. Requires deck_pool and mp_start_method=fork.
curriculum:
  enabled: false
  # Maximum matchups scored at once. When it covers the whole corpus (>= the
  # square of the archetype count) every matchup is prefilled up front and
  # nothing is ever evicted. Below that, the corpus is too large to fit -- set
  # explore_prob > 0 so the rest is discovered lazily instead of raising.
  capacity: 4000
  # Probability a worker draws a fresh, uniformly random matchup instead of
  # replaying from the published distribution, so matchups beyond `capacity`
  # keep getting discovered and scored throughout the run rather than only
  # once, before the first publish. Required (> 0) whenever the corpus has
  # more archetype pairs than `capacity`; 0 (default) is the old
  # everything-prefilled behaviour, appropriate when it fits.
  explore_prob: 0.0
  # Rank-distribution temperature; lower is greedier toward top-scoring levels.
  score_temperature: 0.9
  # Weight of the staleness term in the sampling mixture. Set above the PLR
  # paper's 0.25 because self-play moves the league underneath a level and ages
  # its score for reasons unrelated to learning, and because staleness is the
  # only thing that recovers a level whose score has gone flat.
  staleness_coefficient: 0.4
  # Episodes a matchup needs before its score is trusted or it may be evicted.
  # A handful of terminal +/-1 outcomes says almost nothing.
  min_visits: 5
  # Score only episodes played against the fixed anchor opponent. Off by
  # default: it removes the drift a strengthening league induces in level
  # scores, but discards most episodes' scoring signal. Turn on only if the
  # logged diagnostic shows the drift matters.
  anchor_only_scoring: false
  # Frames between dumps of the buffer's win/loss tallies. Deck selection needs
  # those over a window of training, which a single end-of-run total cannot
  # provide. Null derives an interval from the batch size.
  state_interval: null
  # Continue a previous run's level buffer from one of those dumps, so a
  # continued run keeps the visit counts and win/loss tallies that decide which
  # matchups the sampler prioritizes instead of rediscovering the corpus. The
  # dump must come from a run over this same deck corpus: a matchup is
  # identified by its position in the archetype list, so a state built over a
  # different list would silently address the wrong matchups, and loading one
  # raises. Null starts with an empty buffer.
  init_state: null
# Padded size of the option space (stop action excluded); must exceed the
# largest option list the engine can offer, or options get truncated.
max_options: 128
# Observation encoder: "structured" (card-ID/option/board tensors, consumed
# model-side via StructuredObsAdapter) is the only supported encoder.
encoder: structured
# Number of environment instances to run, each with its own battle handle.
num_workers: 16
# Collect with a multiprocess ParallelEnv (true) or a single-process SerialEnv (false).
parallel: true
# Multiprocessing start method for ParallelEnv workers (e.g. fork, spawn).
mp_start_method: fork
# When parallel is true but num_workers is 1, fall back to a single-process env
# instead of paying multiprocessing overhead for one worker.
serial_for_single: true
