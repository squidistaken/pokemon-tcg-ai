# Template for the untracked .env in the repo root:  cp .env.example .env

# Weights & Biases API key, from https://wandb.ai/authorize
# W&B is the default backend. Without a key runs record offline (sync them later
# with `wandb sync`); `wandb login` works too, and `logging=none` opts out.
WANDB_API_KEY=
