[project]
name = "pokemon-tcg"
version = "0.1.0"
description = "RL-based solution for pokemon-tcg"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "hydra-core>=1.3.4",
    "kagglehub>=1.0.2",
]
